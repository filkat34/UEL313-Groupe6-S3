{% extends "layout.html.twig" %}

{% block title %}Erreur !{% endblock %}

{% block content %}
<div class="row">
    <div class="col-xs-12 col-sm-8 col-sm-offset-2 text-center">
        <div class="page-header">
            <h1>Whoops...</h1>
        </div>

        <div class="panel panel-danger">
            <div class="panel-body">
                <img class="img-responsive center-block" src="{{ app.request.basepath }}/images/404-ghost.png" alt="Error ghost" style="max-width: 300px; margin-bottom: 20px;"/>
                <h3>{{ message }}</h3>
                <a href="{{ path('home') }}" class="btn btn-primary btn-lg">
                    <span class="glyphicon glyphicon-home"></span> Retour à l'accueil
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
